name: Quick experiment validation

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'requirements.txt'
      - '.github/workflows/quick.yml'

jobs:
  quick:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies (pin jax to known-good version)
        run: |
          python -m pip install --upgrade pip
          printf 'jax==0.4.35\njaxlib==0.4.35\n' > /tmp/constraints.txt
          pip install -c /tmp/constraints.txt -r requirements.txt

      - name: Show environment
        run: |
          python -c "import jax, numpy; print('jax', jax.__version__); print('numpy', numpy.__version__)"
          python -c "import pymdp; print('pymdp imported from', pymdp.__file__)"

      - name: Run --quick suite
        run: python src/experiments/run_all.py --quick

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: quick-results
          path: results/
          if-no-files-found: warn
